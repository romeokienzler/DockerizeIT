---
  apiVersion: v1
  kind: Pod
  metadata:
    name: spark
    labels:
      app: spark
  spec:
    containers:
      - name: spark
        image: romeokienzler/spark-py:v2.3.0
        command: ["/opt/spark/bin/spark-shell"]
